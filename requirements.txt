# GUI框架
PySide6>=6.5.0

# 图像处理
opencv-python>=4.8.0
numpy>=1.24.0

# AI推理
rknnlite>=1.6.0

# 语音合成
pyserial>=3.5
pyttsx3>=2.90

# 音频播放
pygame>=2.5.0

# Linux高质量TTS引擎 (可选)
edge-tts>=6.1.0

# Windows语音支持 (仅Windows需要)
pywin32>=306; sys_platform == "win32"

# 网络请求
requests>=2.31.0

# 数据处理
Pillow>=10.0.0
speechrecognition>=3.10.0 ; sys_platform == "win32" or sys_platform == "linux"
PyAudio>=0.2.14 ; sys_platform == "win32" or sys_platform == "linux"
sounddevice>=0.4.6
vosk>=0.3.45 ; sys_platform == "linux"

# 测试框架
pytest>=7.4.0
pytest-qt>=4.2.0

# 代码质量
black>=23.0.0
flake8>=6.0.0

# 可选依赖
# RPi.GPIO>=0.7.0  # 仅在树莓派上需要 